# Game Review App
A simple web application for reviewing, rating and browsing various games


For development on windows/mac just use laravel herd. Site will run in either .test domain.
For development on linux use "./vendor/bin/sail up -d" inside project folder. Site will run in localhost.
